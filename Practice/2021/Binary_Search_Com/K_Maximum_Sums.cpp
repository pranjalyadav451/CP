/*
	We have to find K sublists with the largest sums possible.
*/
#include <bits/stdc++.h>
using namespace std;

void solve() {
	int N; cin >> N;
	vector<int> arr(N);
	for (int &a : arr) {
		cin >> a;
	}
}

int main() {
	int tt;
	cin >> tt;
	while (tt--) {
		solve();
	}
}