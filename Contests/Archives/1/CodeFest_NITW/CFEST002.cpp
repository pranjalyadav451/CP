#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        //cout << t;
        int n, a, b, c;
        cin >> n >> a >> b >> c;
        int ratio = b / a;
    }
    return 0;
}