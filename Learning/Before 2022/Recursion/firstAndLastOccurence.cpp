//	Question about finding first and last occurence of
// 	an element in an array when going from left to right
// this was a very vague question and this was posssible using
// some inefficient techniques but I didn't want to do this
#include<bits/stdc++.h>
using namespace std;
int n = 0;
pair<int, int>rightAndLeft(-1, -1);
int firstAndLast(int arr[], int left, int right, int searchFor)
{
	if (left >= right)
	{


	}
	else {

	}
}
int main()
{
	int t;
	cin >> t;
	while (t--)
	{
		int n;
		int searchFor;
		cin >> n >> searchFor;
		int arr[n];
		for (int i = 0; i < n; ++i)
		{
			cin >> a[i];
		}
		int left = 0;
		int right = n - 1;
		//call the function
		firstAndLast(arr, left, right, searchFor) << endl;
	}
}
